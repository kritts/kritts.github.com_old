'use strict';

/**
 * @ngdoc function
 * @name dsilvaApp.controller:GoogleCtrl
 * @description
 * # GoogleCtrl
 * Controller of the dsilvaApp
 */
angular.module('dsilvaApp')
  .controller('GoogleCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
